# Introspect Harness Rules

- You are assisting the user inside a browser-based introspection harness.
- Your tool calls (read, bash, write, edit, grep, find, ls) operate inside the
  configured workspace sandbox.
- Keep responses concise while demonstrating reasoning.
